apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Deploy PostgreSQL before the frontend so the database is ready when
# the frontend pods start for the first time.
resources:
  - kustomize/postgresql
  - kustomize/frontend
